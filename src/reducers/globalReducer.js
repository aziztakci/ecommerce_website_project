import homeData from "../mockdatas/homeData";


const initialState = {
  user: null, 
};

export const globalReducer = (state = initialState, action) => {
  switch (action.type) {
   
    default:
      return state;
  }
};